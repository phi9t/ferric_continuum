;;; Directory local variables for Ferric Continuum.
;;;
;;; Open repository files read-only by default in Emacs. Toggle per buffer with
;;; `read-only-mode' when editing is intentional.

((nil . ((buffer-read-only . t))))
