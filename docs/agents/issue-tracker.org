#+TITLE: Legacy tracker history

Kata is authoritative for active engineering intent. Follow
=docs/agents/active-work.md=. Existing org-mode files under =.scratch/= are
read-only cutover history: do not create, claim, resolve, update, or dual-write
active work in them. The legacy importer may read them for provenance but never
modifies or deletes them.
