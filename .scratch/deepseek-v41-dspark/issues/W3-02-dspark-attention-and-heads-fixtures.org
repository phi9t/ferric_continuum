#+TITLE: W3-02 DSpark attention + Markov/confidence head op fixtures
#+TYPE: task
#+STATUS: done
#+BLOCKED_BY: W3-01-dspark-config-and-source-verifier
#+PARENT: ../spec.org

* Context

Port the DSpark-specific ops with isolated fixtures before wiring the head
model. These are small variants of already-ported ops.

Read first: =../spec.org=, =../issues/D2-executed-vs-represented.org=,
upstream =DSparkAttention.forward= (model.py 1032-1074),
=get_dspark_topk_idxs= (1020-1029), =DSparkMarkovHead= (1077-1086),
=DSparkConfidenceHead= (1089-1097), and the existing Wave-1
=src/deepseek_v41/sparse.rs= / =attention.rs=.

* Requirements

- =get_dspark_topk_idxs= equivalent: build the window+block index vector in
  Rust; unit-test against a hand-computed fixture for a small window/block.
- DSpark sparse-window attention: reuse the Wave-1 CPU/f32 sparse-window helper;
  add the main_kv prefill-seeding path and the decode-time cache read. Fixture:
  tiny q/kv + attn_sink -> known output.
- Markov head: embed + head over =dspark_markov_rank=; returns =(logits_bias,
  markov_embed)=. Fixture-tested.
- Confidence head: fp32 Linear over =concat(hidden, markov_embed)= -> scalar.
  Must stay fp32. Fixture-tested.
- Fixtures generated by extending =deepseek_v41_fixture_gen.py= with a
  =--family dspark= that computes reference values via the same stubbed upstream
  import used elsewhere (or pure-numpy where the op is stub-free).

* Out of scope

- No =forward_spec= assembly (W3-03). No checkpoint loader (W3-04).

* Verification

- =bazel test //ferric_continuum/tnsr:deepseek_v41_math_tests= green.
- Each op fixture matches reference within Wave-1 tolerances.

* Comments

* Answer

Closed by canonical DeepSeek V4.1 issue
=ferric_continuum/tnsr/docs/deepseek_v41/issues/22-dspark-attention-and-head-op-fixtures.org=.
That issue records DSpark attention, Markov head, and confidence head fixture
coverage as done, with =deepseek_v41_math_tests= passing. This scratch ticket is
historical scaffolding and should not be reopened as executable work.
