#+TITLE: Reconcile selected parallelism reports
#+TYPE: task
#+STATUS: resolved
#+BLOCKED_BY: 11-add-scaling-memory-reports
#+PARENT: ../spec.org

* Context

Evaluate `/data02/home/philip.yang/workspace/tnsr/scaling/parallelism.rs` only for concepts not already covered by `ferric_continuum/tnsr/src/scaling/distributed/*`.

The preferred destination is small additions to existing distributed scaling modules. A new `scaling/parallelism.rs` should be added only if the concept is genuinely cross-cutting and cleaner outside `distributed`.

* Requirements

- Inventory external parallelism report concepts against existing `scaling/distributed/*`.
- Fold complementary formulas or summaries into the current modules where possible.
- Add `ferric_continuum/tnsr/src/scaling/parallelism.rs` only if the implementation justifies a separate cross-cutting module.
- Add the new source file to `_TNSR_SRCS` only if a new module is created.
- Add or update Bazel tests in `scaling_tests`, `distributed_tests`, or a focused target.
- Keep naming and semantics aligned with existing DDP, FSDP/ZeRO, tensor parallel, and pipeline parallel modules.

* Out of scope

- Introducing a second competing model of DDP, FSDP, tensor parallel, or pipeline parallel behavior.
- Moving large existing distributed modules for cosmetic reasons.
- Importing external scaling files wholesale.

* Verification

#+begin_src sh
bazel build //ferric_continuum/tnsr:tnsr
bazel test //ferric_continuum/tnsr:lib_tests
bazel test //ferric_continuum/tnsr:scaling_tests
bazel test //ferric_continuum/tnsr:distributed_tests
#+end_src

If public docs change:

#+begin_src sh
bazel build //ferric_continuum/tnsr:tnsr_doc
#+end_src

* Comments

* Answer

- Implemented in `ferric_continuum/tnsr/src/scaling/distributed/report.rs` without adding a new `scaling/parallelism.rs`.
- Folded the external aggregate concepts into the existing distributed report:
  - DDP gradient all-reduce was already covered by `data_parallel`.
  - FSDP / ZeRO parameter all-gather and gradient reduce-scatter were already covered by `fsdp`.
  - Tensor-parallel row all-reduce was already covered by `tensor_parallel`.
  - Pipeline-parallel scheduling remains owned by `distributed::pipeline`; the aggregate report now exposes both schedule microbatch count and derived microbatch size instead of overloading the external `microbatch_per_pipeline_stage` summary.
  - Pipeline handoff byte totals are reported as step-level logical bytes across adjacent stage boundaries and schedule microbatches.
  - Sequence/context parallel degree is summarized as an activation shard factor and symbolic K/V exchange without adding a byte model.
- Added `test_distributed_report_summarizes_hybrid_parallelism` to `distributed_tests`.
- Added `test_pipeline_handoff_uses_largest_uneven_microbatch` to keep `pipeline_schedule` conservative for uneven batch splits.
- Verification in `/data02/home/philip.yang/workspace/ferric_continuum/.worktrees/incorporate-tnsr-12`:
  - `/data02/home/philip.yang/.local/bin/bazel-9.2.0 build //ferric_continuum/tnsr:tnsr`
  - `/data02/home/philip.yang/.local/bin/bazel-9.2.0 test //ferric_continuum/tnsr:lib_tests //ferric_continuum/tnsr:scaling_tests //ferric_continuum/tnsr:distributed_tests //ferric_continuum/tnsr:scaling_memory_tests`
  - `/data02/home/philip.yang/.local/bin/bazel-9.2.0 build //ferric_continuum/tnsr:tnsr_doc`
  - `git diff --check`
- Independent review by separate-context `openrouter-3o` reviewer found no blockers after the pipeline handoff accounting fix.
- Verification repeated in the primary checkout with the same commands; all passed. `tnsr_doc` still reports pre-existing rustdoc warnings in `autograd.rs` and `qwen3_load.rs`.

Full Ferric code review follow-up:

- A later clean-context review of the full incorporation diff found one
  important issue in this ticket: the aggregate report double-counted gradient
  synchronization for ZeRO-2/3 by adding both the DDP gradient all-reduce and
  the FSDP gradient reduce-scatter.
- Fixed `distributed_report` so total communication is stage-aware:
  - ZeRO-1 counts DDP gradient all-reduce.
  - ZeRO-2/3 count FSDP communication instead, which includes gradient
    reduce-scatter and, for ZeRO-3, parameter all-gather.
  - TP all-reduce and PP handoff bytes remain additive.
- Updated symbolic collectives so ZeRO-3 no longer lists a DDP gradient
  all-reduce alongside FSDP gradient reduce-scatter.
- Added `test_distributed_report_uses_ddp_gradient_sync_for_stage1_only` and
  updated the hybrid Stage3 total-communication assertion.
- Focused re-verification:

#+begin_src text
/data02/home/philip.yang/.local/bin/bazel-9.2.0 test //ferric_continuum/tnsr:scaling_memory_tests //ferric_continuum/tnsr:distributed_tests
Test cases: finished with 2 passing, 0 skipped and 0 failing out of 2 test cases.
#+end_src

- Scoped re-review after the fix found no remaining Critical, Important, or
  Minor issues.
